spring:
  datasource:
    url: jdbc:mysql://localhost:3355/cabinet_medical?useSSL=false&serverTimeeone=UTC&useLegacyDatetimeCode=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

  logging:
    level:
      root: WARN
      fr.semifir: INFO
    file: ./logs.log.txt
